name: Build Android APK

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build Debug APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Fix Gradle wrapper line endings
        working-directory: New folder
        run: |
          sed -i 's/\r$//' gradlew
          chmod +x gradlew
          head -n 1 gradlew

      - name: Build debug APK
        working-directory: New folder
        run: ./gradlew assembleDebug --stacktrace

      - name: Verify APK exists
        run: |
          test -f "New folder/app/build/outputs/apk/debug/app-debug.apk"
          ls -lh "New folder/app/build/outputs/apk/debug/app-debug.apk"

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: New folder/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 14
